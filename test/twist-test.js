/*global assert: false, refute: false */
var buster = require("buster"),
    huddle = require("../lib/twist.js"),
    resources;

buster.testCase('Twist', {
    setUp: function () {
        // resources = new twist.Twist();
    },

    "Dummy": function () {
        assert(true);
    }
});
